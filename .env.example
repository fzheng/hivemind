OWNER_TOKEN=dev-owner
NATS_URL=nats://nats:4222
DATABASE_URL=postgresql://hlbot:hlbotpassword@postgres:5432/hlbot

# hl-scout
SCOUT_PORT=4101
# SCOUT_SEEDS= # Optional: comma-separated addresses to seed when DB is empty
LEADERBOARD_API_URL=https://hyperbot.network/api/leaderboard/smart
LEADERBOARD_TOP_N=1000
LEADERBOARD_SELECT_COUNT=12
LEADERBOARD_PERIODS=30
LEADERBOARD_DEFAULT_PERIOD=30
LEADERBOARD_PAGE_SIZE=100
# Sort order: 0=WinRate, 1=AccountValue, 3=PnL, 4=Trades, 5=ProfitableTrades, 6=LastOp, 7=AvgHold, 8=Positions
LEADERBOARD_SORT=3
LEADERBOARD_ENRICH_COUNT=12
LEADERBOARD_STATS_CONCURRENCY=4
LEADERBOARD_SERIES_CONCURRENCY=2

# hl-stream
STREAM_PORT=4102
SCOUT_URL=http://hl-scout:8080
SAGE_URL=http://hl-sage:8080
LEADERBOARD_WATCH_PERIOD=30

# hl-sage
SAGE_PORT=4103
# Bandit algorithm (Thompson Sampling) configuration
BANDIT_POOL_SIZE=50              # Candidate pool size from leaderboard
BANDIT_SELECT_K=10               # Number of traders to actively follow
BANDIT_MIN_SAMPLES=5             # Minimum signals before trusting posterior
BANDIT_DECAY_FACTOR=0.95         # Weight recent performance (0.5-1.0)

# hl-decide
DECIDE_PORT=4104

# hl-stream
PRICE_SNAPSHOT_INTERVAL_MS=60000 # Price history storage interval (1 min)
